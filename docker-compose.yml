services:
  locaitra-translate:
    build: .
    ports:
      - "8787:8787"
    env_file:
      - .env
    volumes:
      - translate-data:/app/data
    restart: unless-stopped

volumes:
  translate-data:
